# 031 - Prototype 06 V1.3-alpha - Premiere page sandbox IA sans provider

Date: 2026-07-03

## Resume executif

Une premiere page sandbox V1.3-alpha a ete creee pour Prototype 06.

Cette page est une interface de cadrage, sans generation IA reelle. Elle affiche le contexte local du manifest `climate-roundtable-demo`, permet de selectionner Clara, Marco ou Ana, montre les donnees qui pourraient etre autorisees vers un provider futur, liste les donnees interdites et prepare une zone de brouillon IA desactivee.

Aucun appel OpenAI n'a ete effectue. Aucun endpoint IA n'a ete cree. Le flux stable Hub V0.9.9 -> Proto06 V1.2.3 reste intact.

## Etat initial Git

Commandes lancees :

```bash
git status --short
git status --short -- prototypes/00-ic-hub
git status --short -- prototypes/06-voice-agent-ic/index-1.2.3.html prototypes/06-voice-agent-ic/script-1.2.3.js prototypes/06-voice-agent-ic/style-1.2.3.css
git diff -- prototypes/00-ic-hub/server/data/runs.json prototypes/00-ic-hub/server/data/sessions.json
```

Resultats :

- etat Git initial : propre ;
- IC-Hub : aucun diff ;
- Proto06 V1.2.3 stable : aucun diff ;
- `runs.json` et `sessions.json` : aucun diff.

## Fichiers crees

- `sandbox-1.3-alpha.html`
- `sandbox-1.3-alpha.css`
- `sandbox-1.3-alpha.js`
- `reports/031_proto06_v1.3-alpha_sandbox_ui_skeleton_report.md`

Aucun fichier stable V1.2.3 n'a ete modifie.

## Heritage Proto06

La page reprend le cadrage V1.3-alpha defini dans le rapport 030 :

- heritage de Proto06 V1.2.3 ;
- reference au manifest `climate-roundtable-demo` ;
- personnages Clara, Marco, Ana ;
- separation stricte avec le parcours etudiant ;
- fallback scenarise conserve comme reference ;
- brouillon IA non publie par defaut.

## Ecran sandbox

URL testee :

```txt
http://127.0.0.1:8788/sandbox-1.3-alpha.html
```

Elements visibles :

- titre : `Sandbox IA Proto06 V1.3-alpha` ;
- badges :
  - `Sandbox IA` ;
  - `Admin seulement` ;
  - `Experimental` ;
  - `Non publie` ;
  - `Ne pas assigner aux etudiants` ;
- avertissement permanent :
  - cette page prepare une experimentation IA ;
  - elle ne publie rien ;
  - elle ne modifie pas le parcours etudiant ;
  - la generation IA est desactivee.

La page ne contient pas de bouton `Publier` et ne contient pas de bouton `Assigner`.

## Donnees affichees

Le fichier local charge est :

```txt
data/proto06-manifests/climate-roundtable-demo.json
```

La page affiche uniquement un resume court :

- titre ;
- themes climatiques ;
- consigne ;
- question commune ;
- personnages ;
- langues.

Elle n'affiche pas le JSON complet du manifest.

## Selection des personnages

Personnages selectionnables :

- Clara / espagnol ;
- Marco / italien ;
- Ana / portugais du Bresil.

Pour chaque personnage, la page affiche :

- role ;
- langue cible ;
- contexte court ;
- tour scenarise humain de reference ;
- mots transparents ;
- comparaison vide avec un futur brouillon IA experimental.

Tests navigateur :

- selection Marco : OK, tour italien affiche ;
- selection Ana : OK, tour portugais du Bresil affiche.

## Donnees autorisees

La carte `Donnees qui pourraient etre envoyees` affiche un contexte minimal :

- `sourceManifestId` ;
- theme ;
- objectif pedagogique ;
- question commune ;
- `characterId` ;
- langue cible ;
- contexte court ;
- contraintes de longueur ;
- mots transparents souhaites.

Ces donnees sont affichees comme preparation d'un futur endpoint serveur-only. Elles ne sont envoyees a aucun provider dans cette version.

## Donnees interdites

La carte `Donnees interdites` affiche :

- texte apprenant ;
- `launchToken` ;
- session ;
- run complet ;
- `run_events` ;
- cle API ;
- `.env` ;
- donnees personnelles ;
- manifest complet si un resume suffit.

Ces elements sont de simples libelles de garde-fous ; aucun secret n'est lu, affiche ou copie.

## Zone brouillon IA

La zone resultat affiche :

```txt
Aucun brouillon genere
publishable: false
reviewStatus: unreviewed-ai-draft
```

Le bouton est present mais desactive :

```txt
Generation IA desactivee dans cette version
```

Aucun faux texte genere n'est affiche. Aucun mock n'est presente comme une generation reelle.

## Garde-fous UX

Garde-fous visibles :

- page separee `sandbox-1.3-alpha.html` ;
- badges permanents ;
- warning de non-publication ;
- bouton IA desactive ;
- statut `publishable: false` ;
- statut `reviewStatus: unreviewed-ai-draft` ;
- rappel de revue humaine obligatoire ;
- absence de publication ;
- absence d'assignation.

## Absence d'appel IA

Verifications :

- aucun appel OpenAI ;
- aucun endpoint IA cree ;
- aucun provider configure ;
- aucun fetch vers `api.openai.com` observe ;
- aucun appel reseau autre que la lecture locale du manifest ;
- aucun `localStorage` ;
- aucune ecriture `run_events`.

Scan statique :

```txt
fetch(...) apparait uniquement pour data/proto06-manifests/climate-roundtable-demo.json
launchToken, run_events et .env apparaissent uniquement dans la liste des donnees interdites
```

## Verifications techniques

```bash
node --check prototypes/06-voice-agent-ic/sandbox-1.3-alpha.js
```

Resultat : OK.

Navigateur :

- page ouverte sur `http://127.0.0.1:8788/sandbox-1.3-alpha.html` ;
- manifest local charge ;
- badges visibles ;
- selection personnages fonctionnelle ;
- bouton generation desactive ;
- aucune erreur console bloquante ;
- aucun appel `api.openai.com`.

Runtime :

```bash
git diff -- prototypes/00-ic-hub/server/data/runs.json prototypes/00-ic-hub/server/data/sessions.json
```

Resultat : aucun diff.

## Confirmation flux stable intact

Verifications :

- IC-Hub : aucun diff ;
- Proto06 V1.2.3 stable :
  - `index-1.2.3.html` sans diff ;
  - `script-1.2.3.js` sans diff ;
  - `style-1.2.3.css` sans diff ;
- aucun endpoint IA modifie ;
- aucun manifest publie modifie ;
- aucun run apprenant cree.

## Etat Git final attendu

Fichiers ajoutes :

```txt
?? prototypes/06-voice-agent-ic/sandbox-1.3-alpha.html
?? prototypes/06-voice-agent-ic/sandbox-1.3-alpha.css
?? prototypes/06-voice-agent-ic/sandbox-1.3-alpha.js
?? prototypes/06-voice-agent-ic/reports/031_proto06_v1.3-alpha_sandbox_ui_skeleton_report.md
```

## Recommandation pour l'etape suivante

Prochaine etape recommandee : ajouter une protection d'acces explicite avant toute vraie generation.

Perimetre prudent :

- garder la page sandbox separee ;
- ajouter un endpoint serveur admin-only de verification d'acces, sans provider ;
- afficher l'etat d'acces dans la sandbox ;
- refuser teacher/student ;
- conserver le bouton generation desactive ;
- ne toujours pas appeler OpenAI.
